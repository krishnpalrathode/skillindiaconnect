{
  "name": "@skillindiaconnect/contract",
  "version": "0.1.0",
  "private": true,
  "description": "OpenAPI 3.1 contract — source of truth for the SkillIndiaConnect API",
  "scripts": {
    "validate": "redocly lint openapi.yaml --format=stylish"
  },
  "devDependencies": {
    "@redocly/cli": "^1.20.0"
  }
}
