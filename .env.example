# Runtime environment
NODE_ENV=development
PORT=3001

# API server port

# PostgreSQL â€” matches docker-compose defaults
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/skillindiaconnect_dev

# Redis â€” matches docker-compose defaults
REDIS_URL=redis://localhost:6379

# Web app â€” URL of the API (consumed by Next.js)
NEXT_PUBLIC_API_URL=http://localhost:3001
